[build]
builder = "NIXPACKS"

[phases.build]
cmds = ["go build -o checkout checkout.go"]

[deploy]
startCommand = "./checkout"
healthcheckPath = "/status"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 3
